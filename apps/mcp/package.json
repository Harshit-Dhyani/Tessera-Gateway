{
  "name": "@tessera-gateway/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "echo 'No build step for MCP server'"
  },
  "dependencies": {
    "@tessera-gateway/core": "workspace:*",
    "@tessera-gateway/router": "workspace:*",
    "@tessera-gateway/observability": "workspace:*",
    "@tessera-gateway/runtime": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "bun-types": "^1.1.0"
  }
}
